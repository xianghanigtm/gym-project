<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/css/style.css">
    <title>XianghanGymProject - Edit Workout</title>
</head>
<body>
    <%- include('navbar') %>
    <div class="container">
        <br>
        <div class="text-center">
            <h2>Edit Workout</h2>
        </div>
        <br>
        <form action="/editWorkouts/<%= workout.workoutId %>" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="exerciseName" class="form-label">Exercise Name</label>
                <input type="text" class="form-control" id="exerciseName" name="exerciseName" value="<%= workout.exerciseName %>" required>
            </div>
            <div class="mb-3">
                <label for="muscleGrp" class="form-label">Muscle Group</label>
                <input type="text" class="form-control" id="muscleGrp" name="muscleGrp" value="<%= workout.muscleGrp %>" required>
            </div>
            <div class="mb-3">
                <label for="reps" class="form-label">Reps</label>
                <input type="text" class="form-control" id="reps" name="reps" value="<%= workout.reps %>" required>
            </div>
            <div class="mb-3">
                <label for="weight" class="form-label">Weight (kgs)</label>
                <input type="text" class="form-control" id="weight" name="weight" value="<%= workout.weight %>" required>
            </div>
            <div class="mb-3">
                <label for="workoutDescription" class="form-label">Workout Description</label>
                <textarea class="form-control" id="workoutDescription" name="workoutDescription" rows="3" required><%= workout.workoutDescription %></textarea>
            </div>
            <div class="mb-3">
                <label for="video" class="form-label">Example Video (Optional)</label>
                <input type="file" class="form-control" id="video" name="video" accept=".mp4">
                <input type="hidden" name="currentVideo" value="<%= workout.exampleVideo %>">
                <p>Current Video: <%= workout.exampleVideo %></p>
            </div>
            <button type="submit" class="btn btn-primary">Update Workout</button>
        </form>
    </div>
</body>
</html>
